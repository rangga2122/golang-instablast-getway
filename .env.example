# App Settings
APP_PORT=3000
APP_HOST=0.0.0.0
APP_DEBUG=false

# Trial Registration
TRIAL_ACTIVE_DAYS=3
TRIAL_MAX_DEVICES=1

# Database (SQLite)
DB_URI=file:storages/whatsapp.db?_foreign_keys=on

# Optional fallback API key for InstaBlast AI
NVIDIA_API_KEY=
